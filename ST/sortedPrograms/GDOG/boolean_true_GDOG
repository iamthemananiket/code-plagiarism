#include<bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    scanf("%d",&t);
    while(t--)
    {
        int n,m,ans=0;
        scanf("%d%d",&n,&m);
        for(int i=1;i<=m;i++)
        {
            ans=max(ans,n%i);
        }
        printf("%d\n",ans);
    }
}
