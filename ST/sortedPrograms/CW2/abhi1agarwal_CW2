#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
ll fac[30];
int main()
{
	int t;
	scanf("%d",&t);
	while (t--)
	{
		ll i,n;
		scanf("%lld",&n);
		double ans=0;
		for (i=1;i<=n;i++)
			ans+=(1.0/(double)i);
		printf("%.3lf\n",ans);
	}
	return 0;
}