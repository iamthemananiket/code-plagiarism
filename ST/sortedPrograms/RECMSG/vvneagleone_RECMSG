#include <iostream>
using namespace std;

int main() {
    int t; cin >> t;
    int sum;
    while(t--) {
        string s;
        cin >> s;
        sum = 0;;
        for( int i = 0; i < s.size(); ++i ) {
            sum += s[i] - 'a' + 1;
        }
        cout << sum << endl;
    }
	return 0;
}
