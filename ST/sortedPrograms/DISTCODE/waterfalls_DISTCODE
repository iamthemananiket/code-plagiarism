#include <bits/stdc++.h>
using namespace std;

int main() {
	int T;
	cin >> T;
	for (int t=0;t<T;t++) {
		string s;
		cin >> s;
		set<string> has;
		for (int i=0;i<s.size()-1;i++) has.insert(s.substr(i,2));
		cout << has.size() << '\n';
	}

	return 0;
}
