#include <iostream>

using namespace std;

int t, n;
double ans;

int main() {
	cin >> t;
	while(t--) {
		cin >> n;
		ans = 0;
		for (int i = 1; i <= n; i++)
			ans += (1.0 * n) / (1.0 * i);
		printf("%.3f\n", ans);
	}
}