#include <cstdio>

main() {
 int T;
 scanf("%d", &T);
 while(T--){
  char s[100001];
  scanf("%s", s);
  int a = 0, b = 0;
  for(int i = 0; s[i]; i ++) if(s[i] == '0') a++; else b ++;
  if(a == 1 || b == 1) puts("Yes");
  else puts("No");
 }
}