#include <cstdio>
#include <cmath>

int main()
{
    int T, N;

    scanf("%d", &T);

    for ( int i = 1; i <= T; i++ )
    {
        scanf("%d", &N);
        printf("%d\n", (int)sqrt( N ) );
    }

    return 0;
}
