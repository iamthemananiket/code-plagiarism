#include<bits/stdc++.h>
using namespace std;



int main()
{
	
	int t;
	cin>>t;
	while(t--)
	{
	string a,b;
	cin>>a>>b;
	int diff=0,x=0;
	for(int i=0;i<a.size();i++)
	 {
	 	if(a[i]=='?' || b[i]=='?')
	 	 x++;
	 	else if(a[i]!=b[i])
	 	 diff++;
	 }
	 cout<<diff<<" "<<x+diff<<endl;
		
	}
}