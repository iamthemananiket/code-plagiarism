#include<bits/stdc++.h>
using namespace std;
int main(){
	int n,m;
	cin>>n>>m;
	vector<int> speeds(n);
	for(int i=0;i<n;i++){
		cin>>speeds[i];
	}
	sort(speeds.rbegin(),speeds.rend());
	cout<<speeds[m-1]<<"\n";
}