#include <bits/stdc++.h>
using namespace std;

bool rep[100005];

int main() {
	int n,i,v;
	cin >> n;
	for (i=0;i<n;i++) {
		scanf("%d",&v);
		rep[v]=1;
	}
	for(i=1;i<=n;i++)
		if(!rep[i])
			printf("%d ",i);
}